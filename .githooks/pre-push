#!/bin/bash

echo "🚀 Pre-push hook: Generating SEO content for all pages..."

# Run the content generator
npm run build > /dev/null 2>&1

# Check if there are any changes to commit
if [[ -n $(git status --porcelain dist/) ]]; then
    echo "✅ New SEO content generated - adding to commit"
    git add dist/
    git commit -m "🤖 Auto-generated SEO content for all pages [skip ci]"
else
    echo "✅ SEO content already up to date"
fi

echo "🎉 Pre-push hook completed successfully"
exit 0
